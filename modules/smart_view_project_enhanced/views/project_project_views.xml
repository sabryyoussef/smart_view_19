<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Project Form View Enhancement -->
    <record id="view_project_form_enhanced" model="ir.ui.view">
        <field name="name">project.project.form.enhanced</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            
            <!-- Add Generate Tasks button -->
            <xpath expr="//header" position="inside">
                <button name="action_generate_tasks" 
                        string="Generate Tasks" 
                        type="object"
                        class="btn-primary"
                        invisible="is_template or generated_tasks_count > 0"
                        help="Generate tasks from templates"/>
                <button name="action_view_generated_tasks" 
                        string="View Generated Tasks" 
                        type="object"
                        class="btn-secondary"
                        invisible="generated_tasks_count == 0"
                        help="View tasks created from templates"/>
            </xpath>

            <!-- Add task templates selection -->
            <xpath expr="//notebook" position="inside">
                <page string="Task Templates" name="task_templates" 
                      invisible="is_template">
                    <group>
                        <group>
                            <field name="auto_generate_tasks"/>
                            <!-- <field name="use_standard_stages"/> Temporarily disabled - Odoo 19 compatibility -->
                        </group>
                    </group>
                    <separator string="Task Templates"/>
                    <field name="task_template_ids" 
                           widget="many2many_tags"
                           placeholder="Select task templates to generate..."/>
                    <group>
                        <p>
                            <b>Note:</b> If no templates are selected, all active standard templates will be used.
                        </p>
                    </group>
                </page>
            </xpath>

            <!-- Add smart button for generated tasks -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_generated_tasks" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-tasks"
                        invisible="generated_tasks_count == 0">
                    <field name="generated_tasks_count" widget="statinfo" string="Generated Tasks"/>
                </button>
            </xpath>

            <!-- Add computed fields -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="generated_tasks_count" invisible="1"/>
            </xpath>

        </field>
    </record>

</odoo>

