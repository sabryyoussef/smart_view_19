<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- Note: These are documentation/examples -->
        <!-- Actual templates must be created in pragtech_whatsapp_base UI and approved by Meta -->
        
        <!-- Template examples are provided in README -->
        <!-- Users need to:
             1. Go to WhatsApp > Templates
             2. Create templates with these names
             3. Get approval from Meta (if using Meta provider)
             4. Then the module will use them automatically
        -->
        
        <!-- Template Name Conventions (for user reference):
        
        1. quotation_template
           - For sending quotations
           - Variables: {{customer_name}}, {{order_name}}, {{order_amount}}
           
        2. so_confirmation
           - For SO confirmation notifications
           - Variables: {{customer_name}}, {{order_name}}
           
        3. project_created
           - For project creation notifications
           - Variables: {{customer_name}}, {{project_name}}
           
        4. payment_received
           - For payment confirmation
           - Variables: {{customer_name}}, {{amount}}, {{order_name}}
        -->

    </data>
</odoo>

